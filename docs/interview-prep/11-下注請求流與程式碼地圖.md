# 下注請求流與程式碼地圖

這份筆記用來回答白板題：

> 前端 -> Gateway(限流 -> JWT -> 玩家限流) -> game-service 下注 -> REST 同步呼叫 wallet debit（樂觀鎖 + 冪等）-> 結算 credit -> 發 Kafka wallet.credit 事件 -> rank 更新排行 / notification 推播。

---

## 1. 一句話總結

這個專案把「資金正確性」放在同步 REST 流程中處理，讓 game-service 必須等 wallet-service 扣款或派彩成功後才繼續；把「排行榜、通知、即時廣播」放到 Kafka 非同步事件處理，避免拖慢下注主流程。

```text
核心流程：Frontend -> Gateway -> game-service -> wallet-service REST -> 回 HTTP response
延伸流程：wallet/game/rank event -> Kafka -> rank / notification -> WebSocket 推播前端
```

---

## 2. 白板請求流

```text
Frontend
  |
  | POST /api/v1/game/slot/spin
  v
Gateway
  |
  | 1. auth/IP rate limit
  | 2. JWT 驗證
  | 3. 寫入 X-User-Id / X-User-Role
  | 4. player/game rate limit
  v
game-service
  |
  | SlotController -> SlotService
  | 產生 roundId / RNG seed
  v
wallet-service REST debit
  |
  | 1. idempotencyKey 檢查
  | 2. 查 wallet
  | 3. 檢查可用餘額
  | 4. @Version 樂觀鎖更新 balance
  | 5. 寫 wallet_transactions
  | 6. 發 Kafka wallet.debit
  v
game-service
  |
  | RNG 結算遊戲結果
  | 如果中獎，REST 呼叫 wallet credit
  v
wallet-service REST credit
  |
  | 1. idempotencyKey 檢查
  | 2. @Version 樂觀鎖更新 balance
  | 3. 寫 wallet_transactions
  | 4. 發 Kafka wallet.credit
  v
game-service
  |
  | 寫 game_rounds.status = SETTLED
  | 發 Kafka game.result
  v
Frontend
  |
  | HTTP response 拿到下注結果與餘額
```

非同步事件流：

```text
wallet.credit / wallet.debit
  -> rank-service
  -> Redis ZSet 更新排行榜
  -> rank.update
  -> notification-service
  -> WebSocket /topic/rank
  -> frontend

game.result
  -> notification-service
  -> WebSocket /user/queue/notifications
  -> frontend

notification.push
  -> notification-service
  -> WebSocket 私人通知或公開廣播
```

---

## 3. 重要觀念

### 3.1 為什麼要限流？

限流是保護系統不要被瞬間流量打爆。

這個專案有兩層：

| 類型 | 目的 | 實作 |
|---|---|---|
| auth/IP 限流 | 防登入、註冊被暴力攻擊 | Spring Cloud Gateway `RequestRateLimiter` |
| player/game 限流 | 防同一玩家短時間狂刷下注 | 自訂 `PlayerRateLimitGlobalFilter` + Redis |

下注不是單純查資料，它會扣款、寫交易、寫遊戲紀錄、發事件，所以需要特別保護。

### 3.2 樂觀鎖和冪等的差別

兩者不是同一件事。

| 機制 | 解決問題 | 專案實作 |
|---|---|---|
| 樂觀鎖 optimistic lock | 多個請求同時改同一個 wallet，避免餘額被改壞 | `Wallet` entity 的 `@Version` |
| 冪等 idempotency key | 同一個請求 timeout 後重試，避免重複扣款或重複派彩 | `wallet_transactions.idempotency_key` |

簡單記：

```text
樂觀鎖：防止「不同請求」併發改壞資料
冪等：防止「同一請求」重試造成重複扣款
```

### 3.3 REST 同步是什麼？

REST 同步表示 game-service 呼叫 wallet-service 後會等待結果。

因為下注能不能成立，取決於 wallet debit 是否成功。這件事不能丟 Kafka 之後慢慢處理，否則玩家可能先看到遊戲結果，但錢其實沒扣成功。

### 3.4 Kafka 非同步是什麼？

Kafka 非同步表示事件發出後，主流程不用等待下游服務處理完。

例如 wallet-service 發出 `wallet.credit` 後，rank-service 可以稍後更新排行榜，notification-service 可以稍後推播，不影響 game-service 回傳下注結果。

這樣比較好擴展，因為每個服務可以獨立增加 instance：

```text
下注壓力大：擴 game-service
錢包壓力大：擴 wallet-service
排行壓力大：擴 rank-service consumer
通知壓力大：擴 notification-service consumer
```

---

## 4. 程式碼地圖

### 4.1 Frontend

| 檔案 | 負責內容 |
|---|---|
| `frontend/src/services/api.js` | 建立 axios instance、設定 `VITE_API_BASE_URL`、自動帶 JWT、處理 401 refresh |
| `frontend/src/services/gameApi.js` | 送出遊戲 API，例如 `POST /api/v1/game/slot/spin` |
| `frontend/src/hooks/useWebSocket.js` | 連線 STOMP over SockJS，訂閱 `/user/queue/notifications` |
| `frontend/src/components/RealtimeBridge.jsx` | 訂閱 `/topic/rank`，把排行榜廣播寫回 Redux |

前端下注主流程：

```text
SlotGame.jsx
  -> gameApi.spinSlot()
  -> api.post('/api/v1/game/slot/spin')
  -> Gateway
```

前端即時推播流程：

```text
RealtimeBridge.jsx
  -> useWebSocket()
  -> /user/queue/notifications
  -> /topic/rank
```

### 4.2 Gateway

| 檔案 | 負責內容 |
|---|---|
| `backend/gateway-service/src/main/resources/application.yml` | 路由設定、auth IP 限流、game/wallet/rank/notification 路由 |
| `backend/gateway-service/src/main/java/com/luckystar/gateway/config/RateLimitConfig.java` | IP key resolver，優先讀 `X-Forwarded-For` |
| `backend/gateway-service/src/main/java/com/luckystar/gateway/filter/JwtAuthenticationGlobalFilter.java` | 驗 JWT、查 Redis 黑名單/停權狀態、寫入 `X-User-Id` / `X-User-Role` |
| `backend/gateway-service/src/main/java/com/luckystar/gateway/filter/PlayerRateLimitGlobalFilter.java` | 依 playerId 做玩家/遊戲限流 |

Gateway 核心流程：

```text
Request
  -> RequestRateLimiter(auth/IP)
  -> JwtAuthenticationGlobalFilter
  -> PlayerRateLimitGlobalFilter
  -> route to game-service / wallet-service / rank-service / notification-service
```

### 4.3 Game Service

| 檔案 | 負責內容 |
|---|---|
| `backend/game-service/src/main/java/com/luckystar/game/controller/SlotController.java` | 接 `POST /api/v1/game/slot/spin` |
| `backend/game-service/src/main/java/com/luckystar/game/service/SlotService.java` | debit -> RNG -> credit -> 寫 `game_rounds` -> 發 `game.result` |
| `backend/game-service/src/main/java/com/luckystar/game/client/WalletClient.java` | 用 REST 同步呼叫 wallet-service 的 internal API |
| `backend/game-service/src/main/java/com/luckystar/game/config/WalletClientConfig.java` | 建立 `RestClient`，預設帶 `X-Internal-Secret` |
| `backend/game-service/src/main/java/com/luckystar/game/entity/GameRound.java` | 遊戲局紀錄 entity，slot 即時 spin 會直接寫 `SETTLED` |
| `backend/game-service/src/main/java/com/luckystar/game/session/GameSessionService.java` | Redis game session，支援 commit-ahead `STARTED / SETTLED` |
| `backend/game-service/src/main/java/com/luckystar/game/kafka/GameResultEventPublisher.java` | 發 `game.result` 給 notification-service |

SlotService 主流程：

```text
spin()
  -> 產生 roundId / serverSeed / clientSeed
  -> walletClient.debit(playerId, bet, "slot-bet-" + roundId, roundId)
  -> RNG 算結果
  -> 如果 payout > 0，walletClient.credit(playerId, payout, "slot-win-" + roundId, roundId)
  -> roundRepository.save(gameRound)
  -> eventPublisher.publishSlotResult(round, outcome)
  -> 回傳 SpinResponse
```

### 4.4 Wallet Service

| 檔案 | 負責內容 |
|---|---|
| `backend/wallet-service/src/main/java/com/luckystar/wallet/controller/InternalWalletController.java` | 提供 `/internal/wallet/debit` 和 `/internal/wallet/credit` |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/service/WalletService.java` | debit/credit 核心邏輯 |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/entity/Wallet.java` | wallet 寫庫 entity，使用 `@Version` 樂觀鎖 |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/entity/WalletTransaction.java` | wallet 交易流水，含 `idempotencyKey` |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/repository/WalletRepository.java` | wallet repository |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/repository/WalletTransactionRepository.java` | 交易流水 repository，查 `idempotencyKey` |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/kafka/WalletReadSyncListener.java` | 消費 `wallet.credit` / `wallet.debit`，同步 MySQL 讀模型 |
| `backend/wallet-service/src/main/java/com/luckystar/wallet/kafka/WalletCreditRequestListener.java` | 消費 `wallet.credit.request` 指令後真正入帳 |

debit 核心流程：

```text
WalletService.debit()
  -> findByIdempotencyKey()
  -> 已存在：直接回原交易結果
  -> walletRepository.findById(playerId)
  -> 檢查 balance - frozenAmount >= amount
  -> wallet.setBalance(balance - amount)
  -> walletRepository.save(wallet)  // @Version 樂觀鎖
  -> walletTransactionRepository.save(tx)
  -> kafkaTemplate.send("wallet.debit")
  -> 回 DebitResponse
```

credit 核心流程：

```text
WalletService.credit()
  -> findByIdempotencyKey()
  -> 已存在：直接回原交易結果
  -> walletRepository.findById(playerId)
  -> wallet.setBalance(balance + amount)
  -> walletRepository.save(wallet)  // @Version 樂觀鎖
  -> walletTransactionRepository.save(tx)
  -> kafkaTemplate.send("wallet.credit")
  -> 回 CreditResponse
```

### 4.5 Rank Service

| 檔案 | 負責內容 |
|---|---|
| `backend/rank-service/src/main/java/com/luckystar/rank/kafka/WalletBalanceChangedConsumer.java` | 消費 `wallet.credit` / `wallet.debit` |
| `backend/rank-service/src/main/java/com/luckystar/rank/service/RankService.java` | 用 Redis ZSet 更新總資產榜、每日贏分榜、好友榜 |
| `backend/rank-service/src/main/java/com/luckystar/rank/kafka/RankUpdatePublisher.java` | TOP10 改變時發 `rank.update` |
| `backend/rank-service/src/main/java/com/luckystar/rank/kafka/NotificationPushPublisher.java` | 發 `notification.push`，例如週榜 TOP3 通知 |
| `backend/rank-service/src/main/java/com/luckystar/rank/controller/RankController.java` | 提供排行榜 REST 查詢 API |

Rank 更新流程：

```text
wallet.credit / wallet.debit
  -> WalletBalanceChangedConsumer
  -> RankService.updatePlayerCoins()
  -> Redis ZSet rank:global:coins
  -> TOP10 有變動時 RankUpdatePublisher 發 rank.update
```

### 4.6 Notification Service

| 檔案 | 負責內容 |
|---|---|
| `backend/notification-service/src/main/java/com/luckystar/notification/config/WebSocketConfig.java` | 設定 STOMP `/ws`、`/topic`、`/queue`、`/user` |
| `backend/notification-service/src/main/java/com/luckystar/notification/security/StompAuthChannelInterceptor.java` | WebSocket CONNECT 時驗 JWT |
| `backend/notification-service/src/main/java/com/luckystar/notification/kafka/GameResultConsumer.java` | 消費 `game.result`，推送玩家私人遊戲結果 |
| `backend/notification-service/src/main/java/com/luckystar/notification/kafka/RankUpdateConsumer.java` | 消費 `rank.update`，廣播 `/topic/rank` |
| `backend/notification-service/src/main/java/com/luckystar/notification/kafka/NotificationConsumer.java` | 消費 `notification.push`，推送私人通知或公開通知 |

推播流程：

```text
game.result
  -> GameResultConsumer
  -> convertAndSendToUser(playerId, "/queue/notifications", payload)

rank.update
  -> RankUpdateConsumer
  -> convertAndSend("/topic/rank", payload)

notification.push
  -> NotificationConsumer
  -> 有 targetPlayerId：/user/{playerId}/queue/notifications
  -> 沒有 targetPlayerId：/topic/notifications
```

---

## 5. 失敗模式與例外處理（面試常見追問）

這段是白板題最常被追問的地方：「如果中間某一步失敗會怎樣？」

### 5.1 錯誤對照表

| 情境 | 觸發位置 | 例外 / 狀態碼 | 結果 |
|---|---|---|---|
| 餘額不足 | `WalletService.debit()` 餘額檢查 | `InsufficientBalanceException` -> HTTP 422 | `WalletClient` 收到 422 轉成 `InsufficientBalanceException`，`SlotService.spin()` 沒接 try/catch，直接往外丟，**RNG 不會執行、`game_rounds` 不會寫**，等於這局從沒發生 |
| wallet-service 逾時 / 連不到 | `WalletClient.post()` | `ResourceAccessException` -> `WalletUnavailableException` | 同上，中止在 debit 或 credit 那一步，不寫 round |
| 併發改同一個 wallet（樂觀鎖衝突） | `WalletRepository.save()` | `ObjectOptimisticLockingFailureException` -> HTTP 409（無 retry） | wallet-service 直接把 409 丟回去，`SlotService` 一樣沒有重試邏輯，請求整個失敗 |
| debit 成功、之後 credit 失敗 | `SlotService.settleInternal()` credit 呼叫（無 try/catch） | 依失敗原因是 422 / 409 / 連線例外 | **已知資金風險缺口**：錢已經扣了，但因為例外會在 `roundRepository.save()`（第 4 步）之前就拋出，所以這局不會寫入 `game_rounds`，玩家等於「錢被扣但沒有對局紀錄」，目前程式沒有補償/退款/reconciliation job |
| 內部密鑰不符 | `InternalSecretFilter`（wallet-service，只攔 `/internal/**`） | HTTP 401 | 用 `MessageDigest.isEqual` 防 timing attack，短路整條 filter chain |
| JWT 黑名單 / 帳號停權 / Redis 查詢失敗 | `JwtAuthenticationGlobalFilter` | HTTP 401 | 這三個 Redis 檢查是 **fail-closed**：Redis 掛掉時 `onErrorResume` 回傳「視為封鎖」，寧可擋掉合法請求也不放行可疑請求 |
| 玩家/遊戲限流超過 | `PlayerRateLimitGlobalFilter` | HTTP 429 + `Retry-After: 1` | 用 Redis `INCR` + 首次 `EXPIRE 1s` 做每秒滑動窗口；**這裡是 fail-open**：Redis 掛掉時放行（跟 JWT filter 的 fail-closed 策略相反，因為限流失敗頂多多扛流量，JWT 驗證失敗放行是安全漏洞） |
| Kafka 發布失敗（`wallet.credit` / `wallet.debit`） | `WalletService` 的 `kafkaTemplate.send()` | 無例外往外拋 | fire-and-forget，回傳的 `CompletableFuture` 沒有 `.get()`/callback，只 catch 序列化例外並 `log.warn`；真正的非同步發送失敗（例如 broker 不可達）完全沒人知道，這是「best-effort」的代價 |

### 5.2 限流實際數字（`application.yml`，可用環境變數覆蓋）

```text
AUTH_RATE_LIMIT_BURST   = 10   (auth/IP 限流，登入/註冊)
PLAYER_RATE_LIMIT_BURST = 20   (一般路徑，依 playerId)
GAME_RATE_LIMIT_BURST   = 10   (/api/v1/game/** 路徑，依 playerId)
```

Redis key：`rate:player:{userId}` / `rate:game:{userId}`，每秒重置。

### 5.3 為什麼「debit 成功但 credit 失敗」目前沒有補償機制？可以怎麼答

先承認現況、再講可能的改法，展現你懂 tradeoff：

> 目前 SlotService 的 debit 和 credit 都沒包 try/catch，中間任何一步失敗都會讓整個請求拋例外，`game_rounds` 也不會落地，所以線上會出現「錢扣了但沒有對局紀錄」的視窗。這其實是接受了一個小機率風險，換取程式碼簡單、同步流程好推理。
>
> 如果要補這個洞，常見做法：
> 1. **Saga / 補償交易**：credit 失敗時，game-service 主動呼叫 wallet 的反向 debit（退款），用同一組 `roundId` 當冪等鍵。
> 2. **對帳 job**：定期掃 `wallet_transactions` 有 debit 沒對應 `game_rounds` 的 roundId，人工或自動退款。
> 3. **outbox pattern**：把 debit/credit/round 寫入包在同一個本地交易 + outbox 事件，避免「呼叫遠端服務成功、本地寫入失敗」的落差。
>
> 目前系統選擇「簡單同步 + 冪等鍵防重複」，可以接受但不是完美方案，這是可以提出的改進點。

---

## 6. 面試回答範本

可以這樣講：

> 這個下注流程會先經過 Gateway。Gateway 會先做 auth/IP 限流，再驗 JWT，驗證成功後把 playerId 放到 `X-User-Id` header 往下游傳，接著再做玩家級或遊戲級限流，避免同一玩家短時間狂刷下注。
>
> 通過 Gateway 後，請求進到 game-service 的 SlotController，再由 SlotService 處理。game-service 不自己改錢包，而是用 REST 同步呼叫 wallet-service 的 internal debit API。因為下注能不能成立取決於扣款是否成功，所以這裡必須同步等待 wallet 回應。
>
> wallet-service debit 會先用 idempotencyKey 查交易流水，如果同一個請求已處理過，就直接回原結果，不會再扣一次。接著查 wallet、檢查餘額，透過 JPA `@Version` 做樂觀鎖更新 balance，避免多個下注請求同時改同一個 wallet 造成餘額錯誤。成功後寫入 wallet_transactions，並 best-effort 發出 `wallet.debit` 事件。
>
> game-service 拿到扣款成功後，才執行 RNG 結算。如果中獎，再同步呼叫 wallet-service credit。credit 一樣有 idempotencyKey 和 `@Version` 樂觀鎖，成功後發 `wallet.credit` 事件。
>
> 最後 game-service 寫入 game_rounds，狀態是 `SETTLED`，並發出 `game.result` 事件。HTTP response 會直接回給前端，所以玩家可以馬上看到下注結果和餘額。排行榜與通知則走 Kafka 非同步，rank-service 消費 `wallet.credit/debit` 更新 Redis 排行榜，notification-service 消費 `game.result` 或 `rank.update` 後透過 WebSocket/STOMP 推播前端。
>
> 總結來說，資金交易走同步 REST 保證當下知道成功或失敗；排行榜與通知走 Kafka 非同步，讓主下注流程短、延遲低，也讓 rank 和 notification 可以獨立擴展。

---

## 7. Discord 可貼版本

````md
**Lucky Star Casino 下注請求流**

```text
Frontend
  -> Gateway
     -> auth/IP 限流
     -> JWT 驗證
     -> 寫入 X-User-Id / X-User-Role
     -> 玩家/遊戲限流
  -> game-service
     -> SlotController
     -> SlotService
     -> REST 同步呼叫 wallet debit
  -> wallet-service
     -> idempotencyKey 防重複扣款
     -> Wallet @Version 樂觀鎖防併發改壞餘額
     -> 寫 wallet_transactions
     -> 發 wallet.debit
  -> game-service
     -> RNG 結算
     -> 若中獎，REST 同步呼叫 wallet credit
  -> wallet-service
     -> idempotencyKey 防重複派彩
     -> Wallet @Version 樂觀鎖
     -> 發 wallet.credit
  -> game-service
     -> 寫 game_rounds = SETTLED
     -> 發 game.result
  -> Frontend 收 HTTP response
```

**非同步事件**

```text
wallet.credit / wallet.debit
  -> rank-service
  -> Redis ZSet 更新排行榜
  -> rank.update
  -> notification-service
  -> WebSocket /topic/rank
  -> frontend

game.result
  -> notification-service
  -> WebSocket /user/queue/notifications
  -> frontend
```

**重點**

- REST 同步：資金交易必須馬上知道成功或失敗。
- Kafka 非同步：排行榜、通知不阻塞下注主流程。
- 樂觀鎖：防多個請求同時改壞同一個 wallet。
- 冪等 key：防 timeout retry 造成重複扣款或重複派彩。
- Redis：限流、game session、rank 排行榜。
- Kafka：事件流與服務解耦。

**核心檔案**

```text
Frontend:
- frontend/src/services/api.js
- frontend/src/services/gameApi.js
- frontend/src/hooks/useWebSocket.js
- frontend/src/components/RealtimeBridge.jsx

Gateway:
- backend/gateway-service/src/main/resources/application.yml
- backend/gateway-service/src/main/java/com/luckystar/gateway/filter/JwtAuthenticationGlobalFilter.java
- backend/gateway-service/src/main/java/com/luckystar/gateway/filter/PlayerRateLimitGlobalFilter.java

Game:
- backend/game-service/src/main/java/com/luckystar/game/controller/SlotController.java
- backend/game-service/src/main/java/com/luckystar/game/service/SlotService.java
- backend/game-service/src/main/java/com/luckystar/game/client/WalletClient.java
- backend/game-service/src/main/java/com/luckystar/game/kafka/GameResultEventPublisher.java

Wallet:
- backend/wallet-service/src/main/java/com/luckystar/wallet/controller/InternalWalletController.java
- backend/wallet-service/src/main/java/com/luckystar/wallet/service/WalletService.java
- backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/entity/Wallet.java
- backend/wallet-service/src/main/java/com/luckystar/wallet/postgres/entity/WalletTransaction.java

Rank:
- backend/rank-service/src/main/java/com/luckystar/rank/kafka/WalletBalanceChangedConsumer.java
- backend/rank-service/src/main/java/com/luckystar/rank/service/RankService.java
- backend/rank-service/src/main/java/com/luckystar/rank/kafka/RankUpdatePublisher.java

Notification:
- backend/notification-service/src/main/java/com/luckystar/notification/config/WebSocketConfig.java
- backend/notification-service/src/main/java/com/luckystar/notification/kafka/GameResultConsumer.java
- backend/notification-service/src/main/java/com/luckystar/notification/kafka/RankUpdateConsumer.java
- backend/notification-service/src/main/java/com/luckystar/notification/kafka/NotificationConsumer.java
```
````
