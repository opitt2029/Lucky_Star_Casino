# 07 — 面試題庫・進階 200 題（201–400・分脈絡）

> 這是 `05` 的續集。`05` 的 1–200 打底，這裡的 **201–400** 走更深、更廣、或換角度追問。
> 用法同 `05`：**蓋住 `08` 的詳解**自己先答；每題括號是「脈絡指引」。
> 8 個新脈絡 **I→P**：補強基礎硬底子（I–L）、深化既有主戰場（M–N）、系統設計（O）、行為軟實力（P）。
> 編號連續 201–400，詳解見 `08-進階解答與詳解.md` 同編號（每題「白話／重點／口說」三段式）。

---

## 脈絡 I — Java 語言與 JVM 深化（201–228）

**簡化講解：** javac 編譯 → bytecode → 類載入（載入→驗證→準備→解析→初始化）→ 解釋/JIT 執行；JIT 探測熱點代碼並編譯機器碼、C1 快速 vs C2 優化；雙親委派防竄改（Bootstrap→Platform→App ClassLoader 逐層問）；物件記憶體佈局：Mark Word(偏向鎖/輕量級鎖標記) + 類別指標 + 實例資料 + 填充；逃逸分析可棧上分配/標量替換/鎖消除；GC 分代：Minor GC（新生代滿）→ Major（老年代滿）→ Full（兜底+清理）；三色標記併發漏標用 SATB/增量更新；G1 Region 化+可預測停頓；ZGC 著色指標+讀屏障+幾乎全併發；引用類：Strong（保活）/ Soft（快取）/ Weak（WeakMap 防洩漏）/ Phantom（追蹤回收）；Full GC 頻繁排查：GC log→jstat→jmap+MAT；OOM 五種（heap/Metaspace/GC overhead/direct memory/request memory）；final 欄位建構完成保證可見性；String.intern() Java 7 後進 heap 自動池；Java 只值傳遞（引用拷貝）；Integer 快取 -128~127、大迴圈自動裝箱浪費；拋例外 fillInStackTrace 成本高；try-with-resources 逆序關閉+suppressed；反射 Method.invoke 慢（跳過 JIT）；註解 SOURCE/CLASS/RUNTIME 保留時機；虛擬執行緒輕量（掛載/卸載 carrier thread）、synchronized 釘住；record/sealed/pattern 搭配做 Kafka 事件不可變。

201-228 簡述：編譯→類載→GC→記憶體→引用→效能→新特性。

---

## 脈絡 J — 並發與多執行緒（229–252）

**簡化講解：** 進程資源單位、線程排程單位；Runnable/Callable/線程池推薦；6 狀態（NEW→RUNNABLE→BLOCKED/WAITING→TERMINATED）；JMM 解決可見性/有序性/原子性，主記憶體+工作記憶體；happens-before（程序序列→鎖→volatile→傳遞）；volatile 記憶體屏障禁重排；synchronized 鎖升級（無鎖→偏向→輕量→重量）；AQS+CLH 佇列；ReentrantLock 公平/非公平；ReadWriteLock 讀多寫少；ThreadLocal WeakKey+remove 防洩漏；線程池流程（核心→隊列→最大→拒絕策略）；BlockingQueue 四種實作；CountDownLatch/CyclicBarrier/Semaphore；jstack 排查死鎖；CompletableFuture 編排非同步；ConcurrentHashMap 1.7 分段鎖 vs 1.8 CAS+synchronized；Executors 無界隊列 OOM；樂觀鎖重試+退避+活鎖；risk:inflight 並發閘。

229-252：線程基礎、JMM、同步原語、並發容器、線程池、死鎖、非同步編排。
235. 為什麼 `i++` 不是原子操作？有哪些修法？（讀-改-寫三步、`AtomicInteger`/`synchronized`）
236. CAS 怎麼運作？有哪三個缺點？（自旋耗 CPU、ABA、只能保證單一變數）
237. `synchronized` 的鎖升級過程是什麼？（無鎖→偏向鎖→輕量級鎖→重量級鎖）
238. `synchronized` 到底鎖的是什麼？monitor 在哪？（物件頭 Mark Word 指向 monitor）
239. AQS（`AbstractQueuedSynchronizer`）的核心思路？（`state` + CLH 雙向佇列）
240. `ReentrantLock` 的公平鎖和非公平鎖差在哪？為什麼預設非公平？（吞吐 vs 飢餓）
241. `ReentrantReadWriteLock` 適合什麼場景？有什麼陷阱？（讀多寫少、寫飢餓、鎖降級）
242. `ThreadLocal` 的原理是什麼？為什麼會記憶體洩漏？（`ThreadLocalMap`、弱引用 key、用完要 `remove`）
243. 執行緒池提交一個任務後的完整流程？（核心執行緒→佇列→最大執行緒→拒絕策略）
244. 執行緒池的四種拒絕策略各是什麼？（Abort/CallerRuns/Discard/DiscardOldest）
245. 常見的阻塞佇列有哪些？差別？（`ArrayBlockingQueue`/`LinkedBlockingQueue`/`SynchronousQueue`）
246. `CountDownLatch`、`CyclicBarrier`、`Semaphore` 分別解什麼問題？（等待/匯合/限流）
247. 死鎖的四個必要條件是什麼？怎麼用 `jstack` 排查？（互斥/持有等待/不可剝奪/循環等待）
248. `CompletableFuture` 怎麼做非同步任務編排？（`thenApply`/`thenCompose`/`allOf`/異常處理）
249. `ConcurrentHashMap` 在 JDK 1.7 和 1.8 的實作差別？（分段鎖 Segment vs CAS+synchronized 鎖桶頭）
250. 為什麼《阿里巴巴規約》禁止用 `Executors` 的工廠方法建執行緒池？（無界佇列/無界執行緒 → OOM）
251. 應用層的樂觀鎖重試，怎麼配合 retry/退避避免活鎖？（接本專案 409 重試）
252. 「同一玩家高併發連續下注」你在後端怎麼擋？（接 `risk:inflight:{playerId}` INCR 並發閘）

---

## 脈絡 K — 資料結構與演算法（253–272）

**簡化講解：** 複雜度 O(1)→O(log n)→O(n)→O(n log n)→O(n²)→O(2ⁿ)；陣列隨機存取快、cache 友善；鏈表增刪快；Stack 遞迴、Queue BFS；紅黑樹比 AVL 插入旋轉少；B+ 樹資料只葉節點+葉鏈表，索引最優；跳表多層索引 O(log n)、Redis ZSet；堆積優先隊列、Top K；雜湊鏈結法 vs 開放定址；一致性雜湊 consistent hashing 最小重映、虛擬節點；布隆濾器 Bloom Filter 偽陽性；二分搜尋邊界+變形；排序穩定性（插快歸不穩）；快排 pivot 隨機避免最壞；DP 最優子結構+重疊；Floyd 快慢指標判環；LRU 雙向鏈表+HashMap；限流滑動視窗+環形緩衝。

253-272：複雜度、容器、排序、查找、DP、最佳化。

---

## 脈絡 L — 網路與 Web 基礎（273–288）

**簡化講解：** OSI 七層→應用/傳輸/網路/連結；TCP 三握（雙向確認+防舊連）四揮（半關閉+TIME_WAIT）；UDP 無序無保證、快（音視訊/DNS）；TCP 可靠靠序號/ACK/滑窗/壅塞；HTTP/1.1 隊頭阻塞→HTTP/2 多工→HTTP/3 QUIC；TLS 握手協商+驗證+金鑰交換；HTTPS 非對稱交換+對稱加密；DNS→TCP→TLS→HTTP→DOM；RESTful 資源導向+HTTP 動詞+狀態碼；cookie/session/JWT（本專案）；CORS preflight OPTIONS；強快取 Cache-Control vs 協商 ETag；WebSocket 全雙工 vs HTTP 輪詢；gateway 反向代理。

273-288：TCP/UDP、HTTP/HTTPS、RESTful、快取、WebSocket。

---

## 脈絡 M — Spring 與微服務深化（289–312）

**簡化講解：** JDK 代理（有介面）vs CGLIB（無介面）；@Transactional REQUIRED（複用）vs REQUIRES_NEW（新交易）；self-invocation 失效用 AopContext.currentProxy() / 注自己；循環依賴三級快取（成品+半成品+ObjectFactory）；建構注入無法解；@Autowired byType→byName→@Qualifier；Spring 事件行程內 vs Kafka 跨服；starter autoconfigure+imports；@Conditional OnClass/OnMissingBean/OnProperty；Filter(Servlet)→Interceptor(MVC)→AOP(方法)；GlobalExceptionHandler 409/400；@Valid @Pattern；服務發現環變 URL；配置中心 .env；分散式 session JWT+Redis；分散式鎖 Redis/ZK 誤刪風險；雪花 Snowflake；API Gateway vs BFF；超時+重試+熔斷 Resilience4j；Idempotency-Key；灰度流量切分；traceId/spanId 全鏈路；WebFlux I/O 密集 vs MVC。

289-312：Spring 深度、DI、事務、快取、代理、微服務三劍客（發現/配置/追蹤）。

---

## 脈絡 N — 帳務 / CQRS / Kafka 進階追問（313–344）★深水區

**簡化講解：** 樂觀鎖重試重讀+退避+上限+409；idempotency_key 業務雜湊 vs UUID；金額最小單位整數 / BigDecimal（new 0.1 bug / equals vs compareTo / scale RoundingMode）；balance_before/after 審計+對帳；轉帳同交易固定序防死鎖；Saga 編排（中央協調）vs 協同（事件鏈）；補償失敗重試+告警人工；TCC 資源預留 vs Saga；避免 2PC（單點+久鎖+吞吐）；Outbox 輪詢頻率延遲 vs DB 壓力、FOR UPDATE SKIP LOCKED；CDC binlog vs Outbox 表；消費端冪等唯一+狀態機+去重表；exactly-once 冪等+事務+讀提交；acks=0(丟)/1(leader)/all(ISR)；ISR 同步副本防丟；rebalance stop-world+重複；lag 監控+加分區+優化；playerId key 保序+熱點傾斜；分區數 並行 vs 句柄；重複投遞防冪等；讀延遲 read-your-writes+強查；主從同步 binlog 單線程；避免跨庫寫；四同步 DTO+兩庫 CHECK+migration；對帳流水 vs 餘額跑批；退款沖正不改原。

313-344：樂觀鎖、冪等、BigDecimal、Saga、Kafka 消費、Outbox、CQRS 延遲、對帳。

---

## 脈絡 O — 系統設計與營運（345–372）

**簡化講解：** 短網址發號+雜湊；秒殺庫存預扣+限流+削峰；分散式限流 Redis+Lua 原子；定時任務分散式鎖 / ShedLock；排程避免重複分散鎖 / 選主；Feed 推 vs 拉 vs 混；快取一致 Cache Aside（先 DB 後刪快取，避免舊值回填）；多級快取 Caffeine+Redis；熱點本地+打散+讀寫分；大 key 阻塞+拆分；RDB 快照 vs AOF 日誌；Redis HA 主從+Sentinel+Cluster；淘汰 LRU/LFU；可觀測性 Metrics+Logging+Tracing；liveness（活）vs readiness（收流）；graceful 停新+處理在途+關池；限流/熔斷/降級/隔離；容量 QPS+存儲+帶寬；P99 排查 GC+查詢+鎖+外部+網路；冪等 API 前禁+業務鍵+UNIQUE；日誌結構化+traceId+分級；A/B 灰度比例+特徵開關；慢查詢 EXPLAIN+索引；分表跨庫 JOIN 歸併+分頁；廣播 fanout 分組+批次+背壓；防重複 前禁+後冪；Redis 掛鑑權 fail-closed、排行降級。

345-372：容量設計、快取策略、Redis、可觀測性、容量、冪等、多級架構。

---

## 脈絡 P — 行為題與軟實力（373–400）

**簡化講解：** 自我介紹身分→專案→亮點→方向（電梯版）；STAR 法成就感/挑戰；負責塊+難題；debug 經驗根因；協作 fork/PR/review；意見不合數據+ADR；學新技 ADR+練習；得意設計理由；大錯誤學習；品質測試+CI；做不完砍延後；模糊需求列假設+問+選項；為什麼後端對數據偏好；職規 3-5 年；離開/選公司；優點+缺點真實；事故止血→定位→修→復盤；跟非技人話術白；任務優先四象限+阻塞；不合理先懂意圖；帶人教法；加班觀；反問題對公司。薪資區間+彈性；不會誠實+推理；一句話技能；欠缺+計劃；錄取理由亮點。

373-400：自介、STAR、協作、軟實力、職規、薪資、反問、應急。

---

### 脈絡題數核對
I:28、J:24、K:20、L:16、M:24、N:32、O:28、P:28 → 合計 **200**（編號 201–400）。
連同 `05` 的 1–200，整套題庫共 **400 題**。
