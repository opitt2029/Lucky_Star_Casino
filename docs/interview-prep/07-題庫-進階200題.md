# 07 — 面試題庫・進階 200 題（201–400・分脈絡）

> 這是 `05` 的續集。`05` 的 1–200 打底，這裡的 **201–400** 走更深、更廣、或換角度追問。
> 用法同 `05`：**蓋住 `08` 的詳解**自己先答；每題括號是「脈絡指引」。
> 8 個新脈絡 **I→P**：補強基礎硬底子（I–L）、深化既有主戰場（M–N）、系統設計（O）、行為軟實力（P）。
> 編號連續 201–400，詳解見 `08-進階解答與詳解.md` 同編號（每題「白話／重點／口說」三段式）。

---

## 脈絡 I — Java 語言與 JVM 深化（201–228）

201. 一段 `.java` 從寫好到在 JVM 跑起來，經過哪些階段？（原始碼→bytecode→類載入→解釋/JIT）
202. 什麼是 JIT 編譯？「解釋執行」和「JIT 編譯執行」差在哪？（熱點探測、C1/C2 分層編譯）
203. 類載入的完整過程有哪幾個階段？（載入→驗證→準備→解析→初始化）
204. 什麼是雙親委派模型？它解決什麼安全問題？（Bootstrap/Platform/App ClassLoader 逐層往上問）
205. 一個類什麼時候才會被「初始化」？（主動引用 vs 被動引用）
206. 一個物件在記憶體裡長怎樣？（物件頭 Mark Word + 類別指標 + 實例資料 + 對齊填充）
207. 物件一定配置在 heap 上嗎？什麼是逃逸分析？（棧上分配、標量替換、鎖消除）
208. Minor GC、Major GC、Full GC 分別在什麼時候觸發？（新生代滿 / 老年代滿 / 各種兜底）
209. 物件怎麼從新生代「晉升」到老年代？（年齡閾值、大物件直入、動態年齡判定）
210. 什麼是三色標記法？併發標記為什麼會漏標？怎麼修？（SATB / 增量更新）
211. G1 收集器的核心設計是什麼？（Region 化、可預測停頓模型、回收價值排序）
212. ZGC 為什麼能把停頓壓到毫秒級？（著色指標 + 讀屏障 + 幾乎全併發）
213. 強引用、軟引用、弱引用、虛引用各用在什麼場景？（`SoftReference` 做快取、`WeakReference` 防洩漏）
214. 線上發現 Full GC 很頻繁，你的排查步驟？（GC log、`jstat`、`jmap` + MAT）
215. 懷疑記憶體洩漏 / 發生 OOM，你怎麼定位？（heap dump、支配樹、找 GC Root 引用鏈）
216. `OutOfMemoryError` 有哪幾種？各代表什麼？（heap / Metaspace / GC overhead / direct memory）
217. `StackOverflowError` 和 `OutOfMemoryError` 差在哪？（堆疊深度 vs 堆積耗盡）
218. `final` 欄位在 JMM 裡有什麼特殊保證？（建構完成的可見性、安全發布）
219. 字串常量池在哪？`String.intern()` 做什麼？（Java 7 後移到 heap、手動入池）
220. 為什麼說「Java 只有值傳遞」？傳物件時傳的是什麼？（傳的是引用的拷貝，不是引用本身）
221. `Integer a=127,b=127; a==b` 為 true 但 128 為 false，為什麼？哪些包裝類有快取？（`IntegerCache` -128~127）
222. 在大迴圈裡用 `Integer sum += i` 有什麼隱藏成本？（自動裝箱產生大量臨時物件）
223. 拋例外的效能成本主要在哪？（填充堆疊軌跡 `fillInStackTrace`）
224. `try-with-resources` 多個資源的關閉順序？什麼是 suppressed exception？（逆序關閉、壓制異常）
225. 反射（Reflection）的原理是什麼？為什麼比直接呼叫慢？（`Method.invoke`、跳過部分最佳化、`setAccessible`）
226. 註解（Annotation）底層怎麼運作？三種保留策略差在哪？（`SOURCE`/`CLASS`/`RUNTIME`）
227. Java 21 的虛擬執行緒底層怎麼跑？什麼是 pinning？（掛載/卸載 carrier thread、`synchronized` 釘住）
228. `record`、`sealed`、pattern matching 三個新特性怎麼搭配用？（接本專案 Kafka 事件 record）

---

## 脈絡 J — 並發與多執行緒（229–252）

229. 行程（process）和執行緒（thread）的本質差別？（資源分配單位 vs 排程單位、記憶體共享）
230. Java 建立執行緒有哪幾種方式？實務上推薦哪種？（`Thread`/`Runnable`/`Callable`/執行緒池）
231. 一個 Java 執行緒有哪些狀態？怎麼轉換？（NEW/RUNNABLE/BLOCKED/WAITING/TIMED_WAITING/TERMINATED）
232. JMM（Java 記憶體模型）在解決什麼問題？（主記憶體 vs 工作記憶體、可見性/有序性/原子性）
233. happens-before 原則大致有哪些？為什麼重要？（程式順序、鎖、volatile、傳遞性）
234. `volatile` 保證可見性和禁止重排，底層靠什麼？（記憶體屏障 memory barrier）
235. 為什麼 `i++` 不是原子操作？有哪些修法？（讀-改-寫三步、`AtomicInteger`/`synchronized`）
236. CAS 怎麼運作？有哪三個缺點？（自旋耗 CPU、ABA、只能保證單一變數）
237. `synchronized` 的鎖升級過程是什麼？（無鎖→偏向鎖→輕量級鎖→重量級鎖）
238. `synchronized` 到底鎖的是什麼？monitor 在哪？（物件頭 Mark Word 指向 monitor）
239. AQS（`AbstractQueuedSynchronizer`）的核心思路？（`state` + CLH 雙向佇列）
240. `ReentrantLock` 的公平鎖和非公平鎖差在哪？為什麼預設非公平？（吞吐 vs 飢餓）
241. `ReentrantReadWriteLock` 適合什麼場景？有什麼陷阱？（讀多寫少、寫飢餓、鎖降級）
242. `ThreadLocal` 的原理是什麼？為什麼會記憶體洩漏？（`ThreadLocalMap`、弱引用 key、用完要 `remove`）
243. 執行緒池提交一個任務後的完整流程？（核心執行緒→佇列→最大執行緒→拒絕策略）
244. 執行緒池的四種拒絕策略各是什麼？（Abort/CallerRuns/Discard/DiscardOldest）
245. 常見的阻塞佇列有哪些？差別？（`ArrayBlockingQueue`/`LinkedBlockingQueue`/`SynchronousQueue`）
246. `CountDownLatch`、`CyclicBarrier`、`Semaphore` 分別解什麼問題？（等待/匯合/限流）
247. 死鎖的四個必要條件是什麼？怎麼用 `jstack` 排查？（互斥/持有等待/不可剝奪/循環等待）
248. `CompletableFuture` 怎麼做非同步任務編排？（`thenApply`/`thenCompose`/`allOf`/異常處理）
249. `ConcurrentHashMap` 在 JDK 1.7 和 1.8 的實作差別？（分段鎖 Segment vs CAS+synchronized 鎖桶頭）
250. 為什麼《阿里巴巴規約》禁止用 `Executors` 的工廠方法建執行緒池？（無界佇列/無界執行緒 → OOM）
251. 應用層的樂觀鎖重試，怎麼配合 retry/退避避免活鎖？（接本專案 409 重試）
252. 「同一玩家高併發連續下注」你在後端怎麼擋？（接 `risk:inflight:{playerId}` INCR 並發閘）

---

## 脈絡 K — 資料結構與演算法（253–272）

253. 大 O 表示法是什麼？把常見複雜度由快到慢排序。（O(1)→O(log n)→O(n)→O(n log n)→O(n²)→O(2ⁿ)）
254. 陣列（array）和鏈結串列（linked list）的取捨？（隨機存取 vs 增刪、cache 友善度）
255. 堆疊（Stack）和佇列（Queue）各自的典型應用？（遞迴/回溯 vs BFS/緩衝）
256. HashMap 為什麼選紅黑樹而不用 AVL 樹做退化結構？（插入旋轉次數、查改平衡）
257. 紅黑樹靠哪些規則維持「近似平衡」？（紅黑五規則、最長路徑 ≤ 2 倍最短）
258. B 樹和 B+ 樹差在哪？為什麼資料庫索引用 B+？（資料只在葉節點、葉節點鏈表、接索引）
259. 跳表（skiplist）的原理是什麼？Redis ZSet 為什麼用它？（多層索引、O(log n)、接排行榜）
260. 堆積（heap）和優先佇列的關係與應用？（完全二元樹、Top K、定時任務）
261. Top K（找前 K 大）有哪些解法？各自複雜度？（小頂堆 O(n log k) / 快速選擇 O(n)）
262. 雜湊衝突有哪兩大類解法？各優缺點？（鏈結法 vs 開放定址）
263. 一致性雜湊（consistent hashing）解決什麼問題？（節點增減時最小化重新映射、虛擬節點）
264. 布隆過濾器（Bloom Filter）的原理和代價？用在哪？（多 hash 位陣列、有偽陽性、接快取穿透）
265. 二分搜尋怎麼處理邊界？常見變形有哪些？（左/右閉、找第一個 ≥ target）
266. 常見排序演算法的時間複雜度和穩定性？（快排/歸併/堆排/插入）
267. 快速排序的原理是什麼？最壞情況怎麼發生、怎麼避免？（pivot 分割、已排序退化、隨機 pivot）
268. 遞迴和迭代怎麼取捨？遞迴有什麼風險？（可讀性 vs 堆疊深度爆掉）
269. 動態規劃的核心思想是什麼？（最優子結構 + 重疊子問題 + 狀態轉移）
270. 怎麼判斷一個鏈結串列有沒有環？（快慢指標 Floyd）
271. LRU 快取怎麼用資料結構實作 O(1)？（雙向鏈結串列 + HashMap，接 Redis 淘汰）
272. 「限流計數」這種實務題你會選什麼結構？（滑動視窗、環形緩衝、接 gateway 限流）

---

## 脈絡 L — 網路與 Web 基礎（273–288）

273. OSI 七層和 TCP/IP 四層大致怎麼對應？（應用/傳輸/網路/連結）
274. TCP 三次握手為什麼要「三次」，兩次不行嗎？（雙向確認收發能力、防歷史連線）
275. TCP 四次揮手為什麼比握手多一次？`TIME_WAIT` 為什麼存在？（半關閉、等遲到封包/確保 ACK 到達）
276. TCP 和 UDP 的差別與選用場景？（可靠有序 vs 快、即時音視訊/DNS）
277. TCP 靠哪些機制保證「可靠傳輸」？（序號+ACK、重傳、滑動視窗流量控制、壅塞控制）
278. HTTP/1.1、HTTP/2、HTTP/3 的主要差別？（隊頭阻塞、多工、二進位、QUIC over UDP）
279. HTTP keep-alive（長連線）解決什麼？（複用 TCP 連線、省握手）
280. HTTPS（TLS）握手大致流程？（協商套件、憑證驗證、交換對稱金鑰）
281. HTTPS 為什麼「非對稱 + 對稱」混用？（非對稱安全交換金鑰、對稱加密資料快；對比 JWT 簽章）
282. 「在瀏覽器輸入網址到看到頁面」中間發生什麼？（DNS→TCP→TLS→HTTP→渲染）
283. RESTful API 的設計原則有哪些？（資源導向、HTTP 動詞、狀態碼、無狀態，接本專案）
284. cookie、session、token 三種狀態保持方式的差別？（接本專案 JWT 無狀態）
285. CORS 跨域的完整機制是什麼？什麼是預檢（preflight）？（簡單/非簡單請求、`OPTIONS`，接 `CORS_ALLOWED_ORIGINS`）
286. 瀏覽器的強快取和協商快取差在哪？（`Cache-Control`/`Expires` vs `ETag`/`Last-Modified`）
287. WebSocket 和 HTTP 輪詢差別？為什麼推播用 WebSocket？（全雙工長連線，接 notification STOMP）
288. 正向代理和反向代理差在哪？gateway 是哪一種？（代理客戶端 vs 代理伺服器）

---

## 脈絡 M — Spring 與微服務深化（289–312）

289. Spring AOP 什麼時候用 JDK 動態代理、什麼時候用 CGLIB？（有介面 vs 無介面、Spring Boot 預設）
290. `@Transactional` 的七種傳播行為各是什麼？最常用哪兩個？（REQUIRED / REQUIRES_NEW / NESTED…）
291. `@Transactional` 的 self-invocation 失效，除了拆 service 還有哪些解法？（`AopContext.currentProxy()`、注入自己）
292. Spring 怎麼解循環依賴？為什麼建構子注入的循環依賴解不了？（三級快取、提早曝光）
293. Spring 的三級快取具體存什麼？（成品/半成品/`ObjectFactory`）
294. `BeanFactory` 和 `ApplicationContext` 差在哪？（延遲 vs 預載、附加企業功能）
295. `@Autowired` 找到多個候選 bean 時怎麼決定注入哪個？（byType→byName→`@Qualifier`/`@Primary`）
296. Spring 的事件機制（`ApplicationEvent`）用在什麼場景？和 Kafka 事件差在哪？（行程內 vs 跨服務）
297. 怎麼自訂一個 Spring Boot starter？（autoconfigure + `AutoConfiguration.imports`）
298. `@Conditional` 家族有哪些常見註解？（`OnClass`/`OnMissingBean`/`OnProperty`）
299. Filter、Interceptor、AOP 三者差別與執行順序？（Servlet 層 vs Spring MVC 層 vs 方法層）
300. `@ControllerAdvice` + `@ExceptionHandler` 怎麼做全域例外處理？（接本專案 `GlobalExceptionHandler` 轉 409/400）
301. `@Valid` / Bean Validation 怎麼運作？（接本專案 DTO 的 `@Pattern` 擋 sub_type）
302. 微服務的「服務發現」是什麼？本專案怎麼做的？（環境變數 URL vs Eureka/Nacos）
303. 配置中心（config server）解決什麼問題？本專案怎麼替代？（集中配置 vs `.env` + 環境變數）
304. 微服務的分散式 session 怎麼處理？（接本專案 JWT 無狀態 + Redis 撤銷層）
305. 分散式鎖有哪些實作方式？各有什麼坑？（Redis SETNX/Redlock、ZooKeeper、過期與誤刪）
306. 分散式唯一 ID 怎麼產生？（雪花算法 Snowflake、UUID、號段模式各取捨）
307. API Gateway 和 BFF（Backend For Frontend）差在哪？（統一入口 vs 為前端聚合）
308. 服務間呼叫的超時、重試、熔斷怎麼搭配？（接 Resilience4j、重試要冪等）
309. 冪等性在 HTTP API 層怎麼設計？（`Idempotency-Key` header、接本專案業務鍵）
310. 灰度發布 / 金絲雀部署在微服務怎麼落地？（按比例切流、可回滾）
311. 微服務怎麼做分散式追蹤？一個 traceId 怎麼貫穿全鏈路？（traceId/spanId、傳遞 header）
312. 為什麼 gateway 用 WebFlux（reactive）而業務服務用 MVC？深入講為什麼「不能在 event loop 上阻塞」？（背壓、少量執行緒撐高併發）

---

## 脈絡 N — 帳務 / CQRS / Kafka 進階追問（313–344）★深水區

313. `debit()` 樂觀鎖衝突後的 retry 機制你會怎麼設計？（重讀最新版本、最大次數、退避，接 409）
314. 樂觀鎖重試到上限還是失敗，最後怎麼回應使用者？（回 409/友善錯誤、不靜默吞）
315. `idempotency_key` 該用什麼生成？UUID 碰撞機率有多低？（業務鍵 hash vs 隨機 UUID）
316. 同一個冪等鍵，第一次處理到一半就崩潰，第二次重送來怎麼辦？（交易未提交 → 無紀錄 → 安全重做）
317. 為什麼金額要用「最小單位整數」或 `BigDecimal`，不能用 `float`/`double`？（浮點誤差、錢不能錯）
318. `BigDecimal` 算金額要注意什麼三個雷？（`new BigDecimal(0.1)`、`equals` vs `compareTo`、scale/`RoundingMode`）
319. `balance_before` / `balance_after` 在對帳時實際怎麼用？（重建帳本、定位斷點）
320. 要做「玩家 A 轉帳給玩家 B」跨帳戶，怎麼保證一致？（同庫同交易 / 跨服務用 Saga）
321. Saga 的兩種模式（orchestration 編排 vs choreography 協同）差在哪？（中央協調器 vs 事件鏈）
322. Saga 的補償交易（compensation）怎麼設計？補償也失敗怎麼辦？（反向操作、重試 + 告警人工）
323. TCC（Try-Confirm-Cancel）和 Saga 差在哪？（資源預留 vs 正向+補償）
324. 為什麼本專案不用 2PC / XA 分散式交易？（協調者單點、鎖資源久、吞吐差）
325. Outbox 表的輪詢頻率怎麼定？太快、太慢各有什麼問題？（延遲 vs DB 壓力，可加事件喚醒）
326. Outbox 怎麼避免「同一筆事件重複發送」？（發送後標記狀態、消費端再冪等兜底）
327. Outbox 輪詢器部署多個實例會不會重複撈同一筆？怎麼解？（`FOR UPDATE SKIP LOCKED` / 分散式鎖）
328. CDC（Change Data Capture，如 Debezium）和 Outbox 輪詢差在哪？（讀 binlog vs 讀表）
329. 消費端做冪等有哪幾種具體做法？（唯一約束去重、狀態機、去重表 + TTL）
330. Kafka 怎麼做到 exactly-once？成本在哪？（冪等 producer + 事務 + 讀已提交）
331. Kafka producer 的 `acks=0/1/all` 差在哪？怎麼取捨？（不等/等 leader/等 ISR）
332. 什麼是 ISR（In-Sync Replicas）？`min.insync.replicas` 有什麼用？（同步副本集、防資料丟失）
333. consumer 的 rebalance 是什麼？會造成什麼問題？（分區重新分配、stop-the-world、重複消費）
334. consumer 處理太慢觸發 rebalance 怎麼解？（`max.poll.interval.ms`、縮小批量、非同步處理）
335. Kafka 訊息積壓（consumer lag）怎麼監控和處理？（監控 lag、加 partition+consumer、優化處理）
336. 為什麼用 `playerId` 當 partition key？出現「熱點玩家」怎麼辦？（保序、熱點傾斜、自訂 partitioner）
337. Kafka 的分區數怎麼定？太多、太少各什麼問題？（並行度 vs rebalance/檔案句柄）
338. `wallet.credit` 事件若被重複投遞，rank 排行會不會重複加分？怎麼防？（消費端冪等去重）
339. CQRS 讀庫有延遲，玩家「下注完馬上查歷史查不到」怎麼處理？（read-your-writes、關鍵查詢打寫庫）
340. 讀寫分離的主從同步怎麼運作？延遲從哪來？（binlog/WAL 複製、單執行緒回放）
341. 雙資料源下「一個操作要同時寫兩個庫」怎麼保證一致？（避免、或用事件最終一致）
342. 為什麼新增 `sub_type` 要「四同步」？漏改其中一處會怎樣？（接雷區 18：DTO `@Pattern` + 兩庫 CHECK + migration）
343. 帳務系統的「對帳（reconciliation）」怎麼設計？（流水 vs 餘額、跑批比對、差異告警）
344. 如果要支援「下注可取消 / 退款」，帳務怎麼設計？（接 `REFUND` sub_type、沖正而非改原紀錄）

---

## 脈絡 O — 系統設計與營運（345–372）

345. 怎麼設計一個短網址（URL shortener）服務？（發號/雜湊、重導 301/302、快取）
346. 怎麼設計一個秒殺 / 限量搶購系統？（庫存預扣、限流、削峰，對比帳務預扣）
347. 怎麼設計一個分散式限流？（Redis + Lua 令牌桶原子扣、接 gateway 限流）
348. 怎麼設計一個分散式定時任務系統？（接 rank scheduler）
349. 多實例部署下排程器怎麼避免「同一個任務跑很多次」？（分散式鎖 / ShedLock / 選主）
350. 怎麼設計一個動態牆 / feed 流？（推模式 vs 拉模式 vs 推拉結合）
351. 快取和資料庫怎麼保證一致？（Cache Aside、延遲雙刪、為何難做到強一致）
352. Cache Aside 為什麼是「先更新 DB 再刪快取」而不是先刪快取？（並發下舊值回填問題）
353. 怎麼設計多級快取？（本地 Caffeine + 分散式 Redis、一致性取捨）
354. 熱點 key 怎麼處理？（本地快取、key 打散分片、讀寫分離）
355. Redis 的大 key / 大 value 有什麼問題？怎麼處理？（阻塞、網路、拆分）
356. Redis 持久化 RDB 和 AOF 差在哪？怎麼選？（快照 vs 命令日誌、混合持久化）
357. Redis 怎麼做高可用？（主從複製、哨兵 Sentinel、Cluster 分片）
358. Redis 的記憶體淘汰策略有哪些？（`noeviction`/`allkeys-lru`/`volatile-lru`/LFU）
359. 系統的可觀測性三本柱是什麼？（Metrics 指標 / Logging 日誌 / Tracing 追蹤）
360. 健康檢查的 liveness 和 readiness 差在哪？（還活著 vs 能收流量，接 actuator）
361. 什麼是優雅關閉（graceful shutdown）？怎麼做？（停收新請求、處理完在途、再關閉連線池）
362. 限流、熔斷、降級、隔離（艙壁）四者差在哪？（控量/斷路/退備援/隔離資源）
363. 怎麼估算一個系統的容量？（QPS、儲存、頻寬的回推估算）
364. 系統出現 P99 延遲尖刺，你的排查思路？（GC/慢查詢/鎖/外部依賴/網路分層排查）
365. 怎麼設計一個端到端冪等的「下注 API」？（前端守門 + 業務冪等鍵 + DB UNIQUE，接整條鏈）
366. 日誌要怎麼設計才好查問題？（結構化、traceId 串接、分級、避免敏感資料）
367. 怎麼做流量切分做 A/B 測試或灰度？（按使用者/比例路由、特徵開關）
368. 資料庫慢查詢怎麼定位和優化？（慢查詢日誌→`EXPLAIN`→索引/改寫/分頁）
369. 分庫分表後，跨庫 JOIN、分頁、排序怎麼處理？（拆分鍵、廣播表、應用層歸併）
370. 怎麼設計「全服廣播」推播給上萬在線玩家？（接 notification fanout、分組/批次/背壓）
371. 怎麼防止使用者「重複提交」（前端連點、網路重送）？（前端禁用 + 後端冪等，接整套防護）
372. 如果 Redis 整個掛了，本專案哪些功能受影響？怎麼降級？（鑑權 fail-closed、排行/限流降級）

---

## 脈絡 P — 行為題與軟實力（373–400）

373. 請做一分鐘自我介紹。（接 `00` 電梯版，結構：身分→專案→技術亮點→想做什麼）
374. 介紹一個你最有成就感 / 最有挑戰的專案。（用 STAR、收斂到帳務正確性）
375. 這個專案你負責哪一塊？遇到最難的問題是什麼？怎麼解的？
376. 講一個你 debug 很久、最後找到根因的經驗。（接捕魚 `fishDamage` 漏序列化）
377. 你怎麼跟團隊協作、做 code review？（接 fork/PR + 至少 1 人 review）
378. 跟同事在技術選型上意見不合，你怎麼處理？（用數據/取捨、ADR 記錄決策）
379. 你平常怎麼學一個新技術？（接這份準備包、寫 ADR、用專案練手）
380. 你寫過最得意的一段程式 / 一個設計是什麼？為什麼？
381. 你犯過最大的技術錯誤是什麼？學到什麼？（接 RTP 門檻訂太低誤判 bug）
382. 你怎麼確保自己的程式碼品質？（接測試分層 + CI 綠燈才開 PR）
383. 時間不夠、做不完時你怎麼取捨範圍？（先正確性/核心路徑，砍可延後的）
384. 面對模糊不清的需求你怎麼辦？（接 CLAUDE.md：先列假設、不懂就問、給選項）
385. 你為什麼想當後端工程師 / DBA？（興趣 + 對正確性/資料的偏好）
386. 你的職涯規劃？三到五年想成為什麼樣的人？
387. 你為什麼想離開現在的位置 / 為什麼選我們公司？
388. 講講你的優點和缺點。（缺點要真實 + 正在怎麼改）
389. 遇到線上事故（production incident）你的處理流程是什麼？（止血→定位→修復→復盤）
390. 怎麼跟非技術的人（PM、老闆）解釋一個技術問題？（用本專案帳務白話舉例）
391. 你怎麼安排多個任務的優先序？（緊急/重要四象限、阻塞別人的先做）
392. 主管交辦的需求你覺得不合理，你會怎麼做？（先理解意圖、提風險與替代、尊重最終決定）
393. 你帶過新人或教過別人嗎？怎麼教的？
394. 你怎麼看待加班和高壓環境？
395. 「你有什麼想問我們的嗎？」你會問什麼？（反問題的策略）
396. 期望薪資怎麼談？（給區間 + 依職責彈性）
397. 如果面試當下有一題你完全不會，你會怎麼處理？（誠實 + 拆解推理 + 講思路）
398. 用一句話總結你最強的技術能力。（接 `00` 數據速記）
399. 你覺得自己目前還欠缺什麼？打算怎麼補？
400. 「為什麼我們應該錄取你？」你會怎麼回答？

---

### 脈絡題數核對
I:28、J:24、K:20、L:16、M:24、N:32、O:28、P:28 → 合計 **200**（編號 201–400）。
連同 `05` 的 1–200，整套題庫共 **400 題**。
