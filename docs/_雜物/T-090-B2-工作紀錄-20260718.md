# T-090 B2 工作紀錄（2026-07-18，Alex 機器）——續跑交接用

> 分支：`feature/alex-t090-b2-debit-roundtrip`。本檔＝中斷點交接紀錄，
> 完成後數據正式併入 `T-090-load-test-report.md` 並更新藍圖 03 的 B2 列，本檔可歸檔至 `docs/_雜物/`。

## 已完成 ✅

1. **B2 實作**（commit `408261f`）：debit 熱路徑 4→2 往返。
   - 新增 `WalletDebitDao`（條件 UPDATE RETURNING＋INSERT ON CONFLICT RETURNING；H2 走 `FINAL TABLE` 等價分流）。
   - `WalletService.debit()` 改寫；冪等/防超扣語意等價論證見 `T-090-B2-debit-roundtrip-design.md`。
   - code-reviewer gate 過：2 must-fix 已修（Kafka 改 afterCommit 發送、CHANGELOG）；判定無資損路徑。
   - 測試：H2 127 全綠、`-Pcontainers-test` 13 全綠（新增真 PG 5 案＋H2 DAO 5 案）。
2. **AGENTS.md 雷區 8 更新**（本 commit）：debit 防超扣改條件 UPDATE 的新約定與禁改事項。
3. **150 併發同機 A/B 對照（本機、全容器化拓樸、新 Postgres volume）**：

   | 輪 | run-id | 版本 | P99 | 樣本 | 5xx/失敗/429 | debit 平均（Prometheus） |
   |---|---|---|---:|---:|---|---:|
   | 暖機（棄置） | 20260718-115442 | B2 | 1300 ms | 17,138 | 0/0/0 | — |
   | 過渡（熱身中，棄置） | 20260718-115800 | B2 | 575 ms | 21,233 | 0/0/0 | 8.6 ms |
   | **B2 正式** | **20260718-120058** | **B2** | **387 ms（PASS 全綠）** | 22,235 | 0/0/0 | **8.57 ms** |
   | 暖機（棄置） | （FAIL 輪） | pre-B2 | 836 ms | — | — | — |
   | **pre-B2 對照** | **20260718-120541** | **develop d8e576b** | **494 ms（PASS）** | 21,692 | 0/0/0 | **10.86 ms** |

   **同機 A/B 結論：B2 讓 150 併發 P99 −21.7%（494→387 ms）、debit 平均 −21%、吞吐 +2.5%。**
   帳務 gate（冪等/超扣斷言）每輪全 0。
4. **重要環境註記（跨輪對照必讀）**：
   - 本機（Alex）≠ 歷輪機器（weiyu）：歷輪 393/377 ms 基線**跨機不可比**。本機 pre-B2 basline＝494 ms。
   - 服務**全容器化**（docker compose，PR #172 拓樸），Postgres volume 為**當日新建**（表基線見下），
     debit 負載 ~240 筆/s 遠低於單機容量 550–600/s——**DB 未飽和**，故 debit 絕對值（~10ms）遠小於
     歷輪飽和時的 194 ms，B2 的差距在未飽和下仍成立但幅度較溫和。
   - 外推（僅供參考、不作驗收依據）：以本機 A/B 比例外推 weiyu 機器 393 ms 基線 → 393×0.783 ≈ **308 ms**，
     落在 250–350 目標帶內；**需在原機器實測才能宣告達標**。
   - JMeter 5.6.3 本機新裝於 `%USERPROFILE%\tools\apache-jmeter-5.6.3`；本機 psql＝`C:\Program Files\PostgreSQL\16\bin\psql.exe`。
   - `.env` 的 DB 帳號是 `lucky_user`/`lucky_star_casino`（壓測準備清單 §3 範例的 `luckystar`/`lucky_star` 是錯的，續跑時別照抄）。
   - provisioning：1,015 名玩家已在 `tests/performance/players.csv`（955 首發＋60 補量；**token 15 分鐘效期，續跑前必跑
     `node tests/performance/refresh-player-tokens.mjs`**）。
   - 1,000 輪前表基線（已記錄）：wallets 1,038 / wallet_transactions 109,695 / game_rounds 82,347。

## 中斷點：剩餘步驟 ⬜（按順序）

1. **1,000 韌性輪（B2）**：wallet 容器目前已是 B2 版（`docker compose up -d --build wallet-service` 已做）。
   B2 版暖機輪已跑（588 ms，棄置）。續跑時：
   ```powershell
   # 確認 15 容器 Up、7 服務 /actuator/health 200 後：
   node tests/performance/refresh-player-tokens.mjs
   .\tests\performance\run-slot-load-test.ps1 -JMeter "$env:USERPROFILE\tools\apache-jmeter-5.6.3\bin\jmeter.bat" -Threads 1000
   # 韌性模式 gate：accepted 成功率 ≥95% ＋ 帳務 0 違規（429/P99 只記趨勢）
   ```
   > 若 docker/服務有重啟過，先跑一輪 `-Threads 150` 暖機棄置＋2.5 分輪距。
2. **T-091 對帳**：
   ```powershell
   .\tests\performance\run-accounting-reconciliation.ps1   # 本機有 psql，直連 localhost:5433
   ```
   判準：9 項 0 新違規（player 1001–1003 歷史髒資料為既知排除項；本輪 volume 是新的，理論上連這 3 筆都不該出現——若出現任何違規一律徹查）。
3. **文件收尾**：
   - `T-090-load-test-report.md` 新增「2026-07-18 B2 對照重跑（Alex 機器）」節（含上表＋1,000 輪＋T-091 結果＋跨機聲明）。
   - 藍圖 `03-T-090-第二輪效能調校藍圖.md` B2 列 🔶→✅（同機 A/B 數據＋選配註記）。
   - CHANGELOG 補「對照重跑結果」條目。
   - （選配）`docs/performance/T-090-壓測前準備清單.md` §3/§7 的 psql 帳號範例更正為 `lucky_user`/`lucky_star_casino`。
4. **Lucky_Star_Prac 校閱**（使用者原始要求第二項）：初步定位的過時點——
   - `99-附錄/99-4-白板速查卡.md` §3 請求流（「冪等鍵預檢 → 餘額守衛 → @Version 樂觀鎖 → UNIQUE 兜底」→ 改為
     「條件 UPDATE（冪等預檢＋守衛＋扣款單語句）→ INSERT ON CONFLICT → 競態補償」）；§4 決策 1（樂觀鎖 vs 悲觀鎖
     的說法要補「debit 已演進為條件 UPDATE，version 欄位保留給其他寫入方」）；§5/§6 壓測數字可補第二輪結案＋B2。
   - `14-1-大型高負載系統優化.md`：故事只講到 C 系列三道閘門，可補第四幕之後的「B1 JFR 定位 DB 往返 → B2 4→2」章節（對 DBA 面試最有料）。
   - `14-6`/`14-17`：原本「條件式 UPDATE 壓成原子操作」的說法**在 B2 落地後才真正成立**——內容不用改，但可加註對應真實檔案 `WalletDebitDao.java`。
   - `05-2/05-3`：教學演進路線（v1 用 @Version）仍然成立，建議只在 05-3 結尾加一段「真專案後來的演進（B2）」前向指標。
   - `99-1` 樂觀鎖問答、`14-9` PostgreSQL 章：可補 `UPDATE ... RETURNING`/`ON CONFLICT`/EvalPlanQual 考點。

## 快速環境自檢（續跑起手式）

```powershell
docker compose ps --format "table {{.Name}}\t{{.Status}}"        # 15 容器（kafka-init Exited 0 正常）
8080,8081,8082,8083,8084,8086,8087 | % { "$_ : " + (Invoke-WebRequest -UseBasicParsing "http://localhost:$_/actuator/health").StatusCode }
git branch --show-current    # 應為 feature/alex-t090-b2-debit-roundtrip
docker exec lucky-star-postgres psql -U lucky_user -d lucky_star_casino -c "SELECT count(*) FROM wallets;"
```
